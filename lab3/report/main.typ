// Главный файл отчета
#import "title.typ": title_page

#set page(margin: 2.5cm)
#set figure(numbering: "1", supplement: [Рисунок])

// Титульная страница
#title_page(
  worktype: [По лабораторной работе],
  theme: [Вариант 16161],
  teacher: [Тюрин Иван Николаевич],
  author: [Ильин Никита],
  group: [P3310],
  date: "2025",
)

#pagebreak()

= Задание

#table(
  columns: 1,
  inset: 12pt,
  stroke: 1pt + black,
  align: left,
)[
  #text(font: ("DejaVu Sans Mono", "Liberation Mono"))[
    #raw(read("zadanie.txt"), block: true)
  ]
]

#v(1cm)

#figure(
  image("ui.png", width: 100%),
  caption: [Пользовательский интерфейс разработанного приложения]
)

#v(1cm)

= Исходный код

#link("https://github.com/MrTheFall/study_is")[https://github.com/MrTheFall/study_is]

#v(1cm)

= UML-диаграммы

#figure(
  image("uml1.png", width: 100%),
  caption: [UML-диаграмма классов]
)

#v(0.5cm)

#figure(
  image("uml2.png", width: 100%),
  caption: [UML-диаграмма пакетов и их зависимостей]
)

#v(1cm)

= Выводы по работе

В ходе выполнения лабораторной работы по дисциплине «Информационные системы» была спроектирована и реализована информационная система для управления сущностью Organization.

На стороне сервера реализованы операции CRUD, проверка бизнес-правил и валидация атрибутов, а также синхронизация состояния с реляционной СУБД PostgreSQL через JPA/Hibernate с генерацией идентификаторов на уровне базы данных.

Пользовательский интерфейс обеспечивает просмотр объектов в табличном виде с пагинацией, сортировкой и фильтрацией по строковым полям, переход к просмотру и редактированию выбранного объекта. Дополнительно реализованы специализированные операции бизнес-логики без прямого использования процедур БД: подсчёт по rating, выборки по префиксу/сравнению поля fullName, выборка топ-5 по годовому обороту и вычисление среднего числа сотрудников для топ-10 организаций.

Архитектура с разделением на уровни представления, бизнес-логики и доступа к данным повышает сопровождаемость и расширяемость решения. Таким образом, цели лабораторной работы достигнуты, получены практические навыки моделирования, проектирования и реализации серверной ИС на базе Spring MVC, JPA/Hibernate и PostgreSQL.

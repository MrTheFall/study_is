// Главный файл отчета
#import "title.typ": title_page

#set page(margin: 2.5cm)
#set figure(numbering: "1", supplement: [Рисунок])

// Титульная страница
#title_page(
  worktype: [По лабораторной работе 3],
  theme: [Вариант 20004],
  teacher: [Тюрин Иван Николаевич],
  author: [Ильин Никита],
  group: [P3310],
  date: "2025",
)

#pagebreak()

= Задание

#table(
  columns: 1,
  inset: 12pt,
  stroke: 1pt + black,
  align: left,
)[
  #text(font: ("DejaVu Sans Mono", "Liberation Mono"))[
    #raw(read("zadanie.txt"), block: true)
  ]
]

#v(1cm)

#figure(
  image("ui.png", width: 100%),
  caption: [Пользовательский интерфейс разработанного приложения]
)

#v(1cm)

= Исходный код

#link("https://github.com/MrTheFall/study_is")[https://github.com/MrTheFall/study_is]

#v(1cm)

= UML-диаграммы

#figure(
  image("uml1.png", width: 100%),
  caption: [UML-диаграмма классов]
)

#v(0.5cm)

#figure(
  image("uml2.png", width: 100%),
  caption: [UML-диаграмма пакетов и их зависимостей]
)

#v(1cm)

= Выводы по работе

Проект получил поддержку массового импорта, с отказоустойчивым хранением импортируемых файлов и кешированием горячих данных.

На бэкенде организовано транзакционное сохранение импортируемых файлов в S3/MinIO с откатом при любой ошибке, а сами данные импортируются с учётом повторного использования координат и адресов. Подключён L2-кэш Hibernate на Ehcache для ускорения повторных запросов к организациям и добавлено аспектное логирование статистики кэша.

У интерфейса появился журнал импортов с возможностью скачать исходный файл напрямую из S3. В совокупности решение демонстрирует применение Spring MVC, JPA/Hibernate, S3-совместимых хранилищ и практик кэширования для повышения производительности и отказоустойчивости.
